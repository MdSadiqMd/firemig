{
  "name": "runable",
  "version": "0.1.0",
  "description": "Reliable Firecracker microVM migration with an Elixir control plane",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint ."
  },
  "engines": {
    "node": ">=22"
  },
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.24.0",
  "devDependencies": {
    "@biomejs/biome": "^2.5.8"
  }
}
